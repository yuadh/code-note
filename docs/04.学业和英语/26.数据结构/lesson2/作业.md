## 作业

## 代码

```c
#include <stdio.h>

int jNumber(int y,int s);
int main(){
    int n,y,s;
    printf("scanf y:");
    scanf("%d",&y);
    printf("scanf s:");
    scanf("%d",&s);
    n = jNumber(y,s);
    printf("the count:%d",n);
}
int jNumber(int y,int s){
    int n;
    if(s==0){
        n=y+1;
    }else{
        n=2*jNumber(y,s-1);
    }
    return n;
}
```

## 流程图

![ns流程图](C:\code-note\docs\04.学业和英语\26.数据结构\lesson2\ns流程图.png)



## 讲解

根据题目，s : 为中间石柱个数 ，y：为中间荷叶个数，n：为最多能转移的青蛙数

输入 `s` 中间石柱数和 `y` 荷叶数，计算得到最多能转移的青蛙数 `n`

**实现**

在无石柱的情况下 ， `s=0` 

归纳法得到的公式 ：`n=y+1`

在有石柱的情况下, 





































